import * as React from 'react';

const BlogPage = ({  }) => {
    return (
        <section>
            {
            }
        </section>
    )
}

import HelloWorld from '../../projects/cronus13/hellow.mdx'
 
export default function projects() {
  return <HelloWorld />
}